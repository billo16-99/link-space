import type { DbClient } from './client';

export function seedDefaultSpaces(_db: DbClient): void {
  // No seed data
}

export function seedDemoLinks(_db: DbClient): void {
  // No seed data
}

export function refreshSeedSpaces(_db: DbClient): void {
  // No seed data
}
